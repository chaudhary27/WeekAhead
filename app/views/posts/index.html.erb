<% @posts.each do |post| %>
  <div>
    <h1><%= post.name %></h1>
    <h2><%= post.content %></h2><br />
    <i><%= post.date %></i>
    <% if post.user.present? %>
      <small> <%= post.user.email %> </small>
    <% end %>
  </div>
  <hr />
<% end %>
